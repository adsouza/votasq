name: shared
description: Data models for Votasq shared across client & server.
version: 0.1.0+2
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  freezed_annotation: ^3.0.0
  json_annotation: ^4.11.0

dev_dependencies:
  build_runner: ^2.13.0
  freezed: ^3.0.0
  json_serializable: ^6.13.1
  test: ^1.25.5
  very_good_analysis: ^10.2.0
